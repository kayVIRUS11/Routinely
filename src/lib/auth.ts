/**
 * Legacy auth config — superseded by Supabase Auth (src/lib/supabase.ts).
 * Kept for any code that hasn't been migrated yet.
 * @deprecated Use AuthContext and src/lib/supabase.ts instead.
 */
export const authOptions = {};
